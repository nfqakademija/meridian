{% extends "::admin.html.twig" %}
{% block title %} Administratoriaus puslapis {% endblock %}

    {% block stylesheets %}
        <!-- BOOTSTRAP STYLES-->
        <link href="{{ asset('assets/css/bootstrap.css') }}" rel="stylesheet">
        <!-- FONTAWESOME STYLES-->
        <link href="{{ asset('assets/css/font-awesome.css') }}" rel="stylesheet">
        <!-- CUSTOM STYLES-->
        <link href="{{ asset('assets/css/custom.css') }}" rel="stylesheet">
        <!-- GOOGLE FONTS-->
        <link href='http://fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'/>
    {% endblock %}
{% block body %}
    <h1 class="text-center">Kurti naują žaidimą</h1>
    <hr>
    <div class="container">
        <div class="row">
            <div class="col-md-4 col-md-offset-4 col-sm-6 col-sm-offset-3 col-xs-10 col-xs-offset-1">
                <div class="panel panel-default border-radius0 text-center">
                    <div class="panel-heading text-center whitespace-bottom">
                        Įveskite duomenis
                    </div>
                    {{ form(form, {attr:{novalidate: 'novalidate' }}) }}

                    {# {{ form_widget(form.email, {'attr': {'class': 'form-control whitespace-bottom', 'placeholder': "El. paštas"}} )}}#}
                </div>
            </div>
        </div>
    </div>
    <hr>
    <div class="text-center">
        <a class="btn btn-danger square-btn-adjust border-radius0 whitespace-bottom" href="{{ path("admin_home_page") }}">Grįžti atgal</a>
    </div>
{% endblock %}