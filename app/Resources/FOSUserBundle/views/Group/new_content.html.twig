{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_group_new') }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_new">
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-danger square-btn-adjust border-radius0" type="submit" value="{{ 'group.new.submit'|trans }}" />
    </div>
</form>
